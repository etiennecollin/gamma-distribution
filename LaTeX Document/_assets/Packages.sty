\usepackage{lipsum}                             % Adds lipsum{} command for testing text
\usepackage{xpatch}								% Used to patch macros and other stuff

\usepackage{fancyhdr}							% For pagestyle, header, footer, etc.
\usepackage{extramarks}							% Used for "Problem continued" in header/footer with fancyhdr
\usepackage{titlesec}                           % Used to change the formatting of sections (centering section title)
\usepackage{setspace}                           % Adds support for text spacing (doublespacing, singlespacing, etc)
\usepackage{xparse}                             % Adds support for multiple optional arguments

\usepackage{amsmath}							% Adds math equations alignment, functions, etc.
\usepackage{amssymb}							% Adds math symbols
\usepackage{amsthm}								% Adds theorems, lemma, proofs, etc.
\usepackage{bm}									% Adds bold font for matrix variables
\usepackage{mathtools}							% Provides multlined environment inside split, equation and align environments.
\usepackage{xfrac}								% Provides better fractions.
\usepackage{empheq}								% Used to put a box around an \align environment
\usepackage[separate-uncertainty=true]{siunitx}	% Provides the \unit{} command for typesetting SI units
\usepackage{cancel}								% Used to cancel units in siunitx
\usepackage[version=4]{mhchem}					% Package for chemical equation typesetting
\usepackage{listings}							% Used to type code using \begin{lstlisting} exactly as written in the document

\usepackage{float}                              % Used to place figures where they appear in source
\usepackage{graphicx}							% Required for the inclusion of images
\usepackage{enumitem}                           % Adds lists

\usepackage[T1]{fontenc}                        % Adds compatibility with accented characters
\usepackage{lmodern}							% Adds support for certain fonts.
\usepackage{slantsc}							% Adds slanted smallcaps
\usepackage{marvosym}							% Adds new symbols

\usepackage{quoting}
\usepackage{csquotes}                           % Adds possibility to get better quoting environments
\usepackage{tocbibind}							% Adds content in TOC. https://ctan.math.ca/tex-archive/macros/latex/contrib/tocbibind/tocbibind.pdf

\usepackage{afterpage}							% Used for "continued" environment
\usepackage{hyperref}							% Adds hyperlinks to TOC to navigate document and ability refer to current section name
\usepackage{caption}                            % Provides better caption management
\usepackage[list=true]{subcaption}              % Provides better subcaption management
\usepackage{tabularx}							% Adds better tables
\usepackage{multirow}							% Adds columns that span multiple rows in tabular environment
\usepackage{diagbox}							% Adds cells with diagonally separated sub-cells

 \usepackage[svgnames, usenames, hyperref]{xcolor}